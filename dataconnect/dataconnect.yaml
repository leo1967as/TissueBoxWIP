specVersion: "v1"
serviceId: "tissueboxwip"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tissueboxwip-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
